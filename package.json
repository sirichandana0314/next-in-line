{
    "name": "next-in-line",
    "version": "1.0.0",
    "description": "A lightweight hiring pipeline that moves itself",
    "scripts": {
        "install:all": "cd server && npm install && cd ../client && npm install",
        "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
        "dev:server": "cd server && npm run dev",
        "dev:client": "cd client && npm start",
        "db:setup": "psql -U postgres -d next_in_line -f server/src/db/schema.sql",
        "db:seed": "psql -U postgres -d next_in_line -f server/src/db/seed.sql",
        "db:reset": "npm run db:setup && npm run db:seed"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}